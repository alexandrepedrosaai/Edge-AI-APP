name: Dynamic WebAssembly Build & Face-Mapped Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install WABT (WebAssembly Binary Toolkit)
        run: |
          sudo apt-get update
          sudo apt-get install -y wabt

      - name: Granular WASM Build
        id: build
        run: |
          mkdir -p docs
          cd WEBASSEMBLY
          
          # Compilar cada função explicitamente para garantir que as dependências do index.html sejam atendidas
          functions=("Functions" "Linear" "Matrixderivate" "Hyperbolic" "Factor" "Unified" "AdvancedLinear" "Ratio")
          
          for func in "${functions[@]}"; do
            if [ -f "${func}.wat" ]; then
              echo "🚀 Compiling Granular Module: ${func}.wat"
              wat2wasm "${func}.wat" -o "../docs/${func}.wasm"
            else
              echo "⚠️ Warning: ${func}.wat not found, skipping..."
            fi
          done
          
          # Compilar quaisquer outros arquivos .wat remanescentes
          for file in *.wat; do
            base=$(basename "$file" .wat)
            if [[ ! " ${functions[@]} " =~ " ${base} " ]]; then
              echo "📦 Compiling Additional Module: $file"
              wat2wasm "$file" -o "../docs/${base}.wasm"
            fi
          done
          
          echo "✅ Build Complete. Artifacts in docs/:"
          ls -lh ../docs/*.wasm

      - name: Patch index.html Cube Visualization
        run: |
          echo "🛠️ Patching cube visualization settings..."
          # Garantir zoom ideal
          sed -i 's/zoom = -[0-9.]\+/zoom = -3.5/g' docs/index.html
          # Garantir limites de rotação (clamping)
          if ! grep -q "const limit = Math.PI / 2 - 0.1" docs/index.html; then
            sed -i 's/rotX += (e.clientY - lastY) \* 0.01;/rotX += (e.clientY - lastY) * 0.01; const limit = Math.PI \/ 2 - 0.1; if (rotX > limit) rotX = limit; if (rotX < -limit) rotX = -limit;/g' docs/index.html
          fi
          echo "✅ Patch applied."

      - name: Validate index.html Linkage
        run: |
          echo "🔍 Checking index.html for WASM references..."
          grep -E "loadWasm\(\".+\.wasm\"\)" docs/index.html || echo "No WASM references found in index.html"

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './docs'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Sync Binaries to Branch
        if: github.event_name == 'push'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add docs/*.wasm
          if ! git diff --cached --quiet; then
            git commit -m "chore: dynamic granular build of WASM modules [skip ci]"
            git push
          else
            echo "No changes in binaries to sync."
          fi
