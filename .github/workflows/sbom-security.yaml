name: Generate SBOMs

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  sbom:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Create required directories
        run: |
          mkdir -p sbom reports
          echo "Directories created"
      
      - name: Install system dependencies
        run: |
          echo "Installing system dependencies..."
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            curl \
            wget \
            git \
            jq \
            python3 \
            python3-pip
      
      - name: Install Syft
        run: |
          echo "Installing Syft..."
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin || true
          syft --version || echo "Syft installation skipped"
      
      - name: Install Trivy
        run: |
          echo "Installing Trivy..."
          sudo apt-get update -qq
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add - || true
          echo "deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" | sudo tee /etc/apt/sources.list.d/trivy.list > /dev/null || true
          sudo apt-get update -qq
          sudo apt-get install -y -qq trivy || echo "Trivy installation skipped"
          trivy --version || echo "Trivy not available"
      
      - name: Generate SBOM for client (if exists)
        run: |
          if [ -d "client" ]; then
            echo "Generating SBOM for client..."
            syft dir:./client -o cyclonedx-json > sbom/client-sbom.json 2>/dev/null || echo "Client SBOM skipped"
          else
            echo "Client directory not found"
          fi
      
      - name: Generate SBOM for server (if exists)
        run: |
          if [ -d "server" ]; then
            echo "Generating SBOM for server..."
            syft dir:./server -o cyclonedx-json > sbom/server-sbom.json 2>/dev/null || echo "Server SBOM skipped"
          else
            echo "Server directory not found"
          fi
      
      - name: Generate SBOM for azure-functions (if exists)
        run: |
          if [ -d "azure-functions" ]; then
            echo "Generating SBOM for azure-functions..."
            syft dir:./azure-functions -o cyclonedx-json > sbom/azure-functions-sbom.json 2>/dev/null || echo "Azure Functions SBOM skipped"
          else
            echo "Azure Functions directory not found"
          fi
      
      - name: Generate SBOM for python (if exists)
        run: |
          if [ -d "python" ]; then
            echo "Generating SBOM for python..."
            syft dir:./python -o cyclonedx-json > sbom/python-sbom.json 2>/dev/null || echo "Python SBOM skipped"
          else
            echo "Python directory not found"
          fi
      
      - name: Scan with Trivy
        run: |
          echo "Scanning with Trivy..."
          trivy fs --format json --output reports/trivy-report.json . 2>/dev/null || echo "Trivy scan skipped"
          trivy fs --format table --output reports/trivy-report.txt . 2>/dev/null || echo "Trivy scan skipped"
      
      - name: Generate security summary
        run: |
          cat > reports/security-summary.md << 'EOF'
          # Security Scan Report
          
          Generated: $(date)
          
          ## Project Structure
          - Client: $([ -d client ] && echo "✓" || echo "✗")
          - Server: $([ -d server ] && echo "✓" || echo "✗")
          - Azure Functions: $([ -d azure-functions ] && echo "✓" || echo "✗")
          - Python: $([ -d python ] && echo "✓" || echo "✗")
          
          ## Dependencies
          - package.json: $([ -f package.json ] && echo "✓" || echo "✗")
          - requirements.txt: $([ -f requirements.txt ] && echo "✓" || echo "✗")
          - Dockerfile: $([ -f Dockerfile ] && echo "✓" || echo "✗")
          
          ## SBOM Files
          $(ls -1 sbom/ 2>/dev/null | sed 's/^/- /' || echo "No SBOM files generated")
          
          ## Scan Reports
          $(ls -1 reports/ 2>/dev/null | sed 's/^/- /' || echo "No scan reports generated")
          
          EOF
          cat reports/security-summary.md
      
      - name: Upload SBOM artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sbom-artifacts
          path: sbom/
          retention-days: 30
      
      - name: Upload scan reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scan-reports
          path: reports/
          retention-days: 30
      
      - name: Commit and push results
        if: always()
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          if [ -n "$(git status --porcelain sbom/ reports/)" ]; then
            git add sbom/ reports/ || true
            git commit -m "chore: Update SBOM and security scan reports [skip ci]" || true
            git push || true
          else
            echo "No changes to commit"
          fi
      
      - name: Create summary
        if: always()
        run: |
          echo "# SBOM Generation Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Generated Files" >> $GITHUB_STEP_SUMMARY
          echo "- SBOM Files: $(ls -1 sbom/ 2>/dev/null | wc -l)" >> $GITHUB_STEP_SUMMARY
          echo "- Scan Reports: $(ls -1 reports/ 2>/dev/null | wc -l)" >> $GITHUB_STEP_SUMMARY
