# This workflow will build and push an application to a Azure Kubernetes Service (AKS) cluster when you push your code
#
# This workflow assumes you have already created the target AKS cluster and have created an Azure Container Registry (ACR)
# The ACR should be attached to the AKS cluster
# For instructions see:
#   - https://docs.microsoft.com/en-us/azure/aks/kubernetes-walkthrough-portal
#   - https://docs.microsoft.com/en-us/azure/container-registry/container-registry-get-started-portal
#   - https://learn.microsoft.com/en-us/azure/aks/cluster-container-registry-integration?tabs=azure-cli#configure-acr-integration-for-existing-aks-clusters
#   - https://github.com/Azure/aks-create-action
#
# To configure this workflow:
#
# 1. Set the following secrets in your repository (instructions for getting these
#    https://docs.microsoft.com/en-us/azure/developer/github/connect-from-azure?tabs=azure-cli%2Clinux):
#    - AZURE_CREDENTIALS
#
# 2. Set the following environment variables (or replace the values below):
#    - AZURE_CONTAINER_REGISTRY (name of your container registry / ACR)
#    - CONTAINER_NAME (name of the container image you would like to push up to your ACR)
#    - RESOURCE_GROUP (where your cluster is deployed)
#    - CLUSTER_NAME (name of your AKS cluster)
#    - IMAGE_PULL_SECRET_NAME (name of the ImagePullSecret that will be created to pull your ACR image)
#
# 3. Choose the appropriate render engine for the bake step https://github.com/Azure/k8s-bake. The config below assumes Kustomize.
#    Set your kustomizationPath and kubectl-version to suit your configuration.
#    - KUSTOMIZE_PATH (the path where your Kustomize manifests are located)
#
# For more information on GitHub Actions for Azure, refer to https://github.com/Azure/Actions
# For more samples to get started with GitHub Action workflows to deploy to Azure, refer to https://github.com/Azure/actions-workflow-samples
# For more options with the actions used below please refer to https://github.com/Azure/login

name: Build and deploy an app to AKS with Kustomize

on:
  push:
    branches: ["main"]
  workflow_dispatch:

env:
  AZURE_CONTAINER_REGISTRY: ${{ secrets.AZURE_ACR_NAME || 'edgeaiacr' }}
  CONTAINER_NAME: "edge-ai-app"
  RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP || 'edge-ai-rg' }}
  CLUSTER_NAME: ${{ secrets.AZURE_AKS_CLUSTER_NAME || 'edge-ai-aks' }}
  KUSTOMIZE_PATH: "k8s"

jobs:
  build-and-deploy:
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    steps:
      # Checks out the repository this file is in
      - uses: actions/checkout@v4

      # Logs in with your Azure credentials
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Discover Resources
        id: discover
        run: |
          echo "Discovering Azure Container Registry..."
          ACR_RG=$(az acr list --query "[?name=='${{ env.AZURE_CONTAINER_REGISTRY }}'].resourceGroup" -o tsv)
          if [ -z "$ACR_RG" ]; then
            echo "::error::ACR ${{ env.AZURE_CONTAINER_REGISTRY }} not found in subscription!"
            exit 1
          fi
          echo "ACR Resource Group: $ACR_RG"
          echo "acr_rg=$ACR_RG" >> $GITHUB_OUTPUT
          
          echo "Discovering AKS Cluster..."
          AKS_RG=$(az aks list --query "[?name=='${{ env.CLUSTER_NAME }}'].resourceGroup" -o tsv)
          if [ -z "$AKS_RG" ]; then
            # Fallback to provided resource group if cluster not found by name alone
            AKS_RG=${{ env.RESOURCE_GROUP }}
            echo "AKS Cluster ${{ env.CLUSTER_NAME }} not found by name, falling back to $AKS_RG"
          fi
          echo "AKS Resource Group: $AKS_RG"
          echo "aks_rg=$AKS_RG" >> $GITHUB_OUTPUT

      # Builds and pushes an image up to your Azure Container Registry
      - name: Build and push image to ACR
        run: |
          az acr build --image ${{ env.AZURE_CONTAINER_REGISTRY }}.azurecr.io/${{ env.CONTAINER_NAME }}:${{ github.sha }} --registry ${{ env.AZURE_CONTAINER_REGISTRY }} -g ${{ steps.discover.outputs.acr_rg }} .

      # Use kubelogin to configure your kubeconfig for Azure auth
      - name: Set up kubelogin for non-interactive login
        uses: azure/use-kubelogin@v1
        with:
          kubelogin-version: 'v0.0.25'

      # Retrieves your Azure Kubernetes Service cluster's kubeconfig file
      - name: Get K8s context
        uses: azure/aks-set-context@v3
        with:
          resource-group: ${{ steps.discover.outputs.aks_rg }}
          cluster-name: ${{ env.CLUSTER_NAME }}
          admin: 'false'
          use-kubelogin: 'true'

      # Runs Kustomize to create manifest files
      - name: Bake deployment
        uses: azure/k8s-bake@v2
        with:
          renderEngine: "kustomize"
          kustomizationPath: ${{ env.KUSTOMIZE_PATH }}
          kubectl-version: latest
        id: bake

      # Deploys application based on manifest files from previous step
      - name: Deploy application
        uses: Azure/k8s-deploy@v4
        with:
          action: deploy
          manifests: ${{ steps.bake.outputs.manifestsBundle }}
          images: |
            ${{ env.AZURE_CONTAINER_REGISTRY }}.azurecr.io/${{ env.CONTAINER_NAME }}:${{ github.sha }}
