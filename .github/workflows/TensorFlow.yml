name: CI/CD TensorFlow Amplified Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  tensorflow:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Python setup
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # Cache datasets
      - name: Cache datasets
        uses: actions/cache@v4
        with:
          path: ~/.keras/datasets
          key: ${{ runner.os }}-datasets

      # Install dependencies
      - name: Install TensorFlow and requirements
        run: |
          pip install --upgrade pip
          pip install tensorflow==2.16.1 flake8 black mypy pip-audit pytest pytest-cov onnx tf2onnx
          pip install -r requirements.txt || true

      # Linting & Type Checking
      - name: Lint & Type Check
        run: |
          flake8 src/
          black --check src/
          mypy src/ --install-types --non-interactive --explicit-package-bases
          pip-audit

      # Unit tests with coverage
      - name: Run Unit Tests
        run: pytest tests/ --cov=src --cov-report=xml

      # Train model
      - name: Train TensorFlow model
        run: |
          mkdir -p dist
          python scripts/train_model.py --epochs 5 --save_path dist/model.h5 > dist/train_output.log

      # Evaluate model with threshold
      - name: Evaluate TensorFlow model
        run: |
          python scripts/evaluate_model.py dist/model.h5 > dist/eval_output.log
          ACCURACY=$(grep "Accuracy:" dist/eval_output.log | awk '{print $2}')
          if (( $(echo "$ACCURACY < 0.90" | bc -l) )); then
            echo "Model accuracy below threshold (90%). Failing build."
            exit 1
          fi

      # Export model formats
      - name: Export model
        run: |
          python scripts/export_model.py dist/model.h5 dist/model.onnx dist/model.tflite

      # Build Docker image
      - name: Build Docker image
        run: |
          docker build -t ghcr.io/${{ github.repository }}/tensorflow:${{ github.run_number }} .

      # Push Docker image
      - name: Push Docker image
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker push ghcr.io/${{ github.repository }}/tensorflow:${{ github.run_number }}

      # Upload artifacts
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: tensorflow-models
          path: dist/*

  release:
    needs: tensorflow
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: tensorflow-models
          path: release

      # Create GitHub Release
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: "Automated TensorFlow Release v${{ github.run_number }}"
          body: |
            Automated build and release of Edge-AI-APP TensorFlow module:
            - Code linted, type-checked, and validated
            - Unit tests executed with coverage
            - Model trained and evaluated (threshold enforced)
            - Exported to H5, ONNX, and TFLite formats
            - Docker image built and pushed to GHCR
            - Logs and artifacts included
          files: release/**
