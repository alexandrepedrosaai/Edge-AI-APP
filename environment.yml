name: base
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - flake8
  - pytest
  - tensorflow
  - pip:
    - tf2onnx
  # Add more dependencies as your project evolves

# Note: pandoc is an external CLI tool; install it separately in your workflow as needed.
# See .github/workflows/python-package-conda.yml for details.
