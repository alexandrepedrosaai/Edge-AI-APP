.text
.globl _start
_start:
    li $t0, 0         # contador
    li $t1, 10        # limite
loop:
    addi $t0, $t0, 1
    bne $t0, $t1, loop

    li $a0, 42        # resultado
    li $v0, 10        # exit syscall
    syscall
