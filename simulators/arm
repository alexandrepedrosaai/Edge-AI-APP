#!/usr/bin/env bash
# arm simulator stub
# Usage: ./simulators/arm --load <binary>
#
# Stub implementation – replace with real ARM simulation when available.

set -e

while [ "$#" -gt 0 ]; do
  case "$1" in
    --load)
      BINARY="$2"
      shift 2
      ;;
    *)
      shift
      ;;
  esac
done

echo "arm simulator: loading '${BINARY}'"
if [ -n "${BINARY}" ] && [ ! -f "${BINARY}" ]; then
  echo "Error: binary '${BINARY}' not found" >&2
  exit 1
fi
echo "arm simulation complete (stub)."
