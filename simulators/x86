#!/usr/bin/env bash
# x86 simulator stub
# Usage: ./simulators/x86 --load <binary>
#
# Stub implementation – replace with real x86 simulation when available.

set -e

while [ "$#" -gt 0 ]; do
  case "$1" in
    --load)
      BINARY="$2"
      shift 2
      ;;
    *)
      shift
      ;;
  esac
done

echo "x86 simulator: loading '${BINARY}'"
if [ -n "${BINARY}" ] && [ ! -f "${BINARY}" ]; then
  echo "Error: binary '${BINARY}' not found" >&2
  exit 1
fi
echo "x86 simulation complete (stub)."
