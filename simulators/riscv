#!/usr/bin/env bash
# riscv simulator stub
# Usage: ./simulators/riscv --load <binary>
#
# Stub implementation – replace with real RISC-V simulation when available.

set -e

while [ "$#" -gt 0 ]; do
  case "$1" in
    --load)
      BINARY="$2"
      shift 2
      ;;
    *)
      shift
      ;;
  esac
done

echo "riscv simulator: loading '${BINARY}'"
if [ -n "${BINARY}" ] && [ ! -f "${BINARY}" ]; then
  echo "Error: binary '${BINARY}' not found" >&2
  exit 1
fi
echo "riscv simulation complete (stub)."
